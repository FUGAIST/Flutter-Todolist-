void main() {
  
  List<int> numbers = [];

  
  numbers.add(10);
  numbers.add(20);
  numbers.add(30);

  
  print(numbers[0]); 

  
  numbers[1] = 25;

  
  numbers.remove(30);

  
  print(numbers.contains(20)); 

  
  print(numbers.length); 

  
  for (var number in numbers) {
    print(number);
  }
  
}
